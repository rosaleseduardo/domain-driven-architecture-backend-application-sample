import { AppResponseLog } from './AppResponseLog';
import { isEqual } from './isEqual';
import { isValidEmail } from './isValidEmail';

export const HELPERS = {
  AppResponseLog,
  isEqual,
  isValidEmail,
};
